import { IntegrationInstance } from '@jupiterone/integration-sdk-core';

const exampleIntegrationInstance: IntegrationInstance = {
  id: 'the-instance-id',
  accountId: 'the-account-id',
  integrationDefinitionId: 'the-integration-definition-id',
  name: 'instance.config.name configured by customer',
  config: {},
};

export default exampleIntegrationInstance;
